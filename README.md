# Springboot Websocket Demo

## Getting started

1. start app: `mvn spring-boot:run`
2. open in browser: `localhost:8080` and supply a user name
3. open in another browser or private window: `localhost:8080` and supply another user name
4. happy chatting :-)
